// TODO: add pointer to functions instead of switchcase

#include "entry_T.h"
#include "app_functions.h"

int main(){
    Entry* first_entry = new Entry;
    createEntryList(first_entry);

    return 0;
}